using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PowerGauges : MonoBehaviour
{
    // TODO: Make one for power, and one for angle launched
    // TODO: Each gauge outputs/returns their value to a function which will calculate velocity (force and direction).
    // TODO: This function will send its data to KidTrajectoryScript, which will launch the kid.
    
    // TODO: Each gauge needs to receive input. Spacebar / Mouse Click.
    
    
    // Start is called before the first frame update
    private void Start()
    {
        
    }

    // Update is called once per frame
    private void Update()
    {
        
    }
}
